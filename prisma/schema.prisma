// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "sqlite"
  url      = "file:./development.db"
}

generator client {
  provider = "prisma-client-js"
}

model ContactType {
  id        Int       @id @default(autoincrement())
  name      String
  createdAt DateTime  @default(now())
  updatedAt DateTime? @updatedAt
  deletedAt DateTime?
  contacts  Contact[] @relation(name: "ContactToContactType")

  @@map("contact_types")
}

model Contact {
  id            Int         @id @default(autoincrement())
  userId        Int
  contactTypeId Int
  title         String
  value         String
  createdAt     DateTime    @default(now())
  updatedAt     DateTime?   @updatedAt
  deletedAt     DateTime?
  user          User        @relation(name: "ContactToUser", fields: [userId], references: [id])
  contactType   ContactType @relation(name: "ContactToContactType", fields: [contactTypeId], references: [id])

  @@map("contacts")
}

model User {
  id                 Int       @id @default(autoincrement())
  contacts           Contact[] @relation(name: "ContactToUser")
  name               String
  jobTitle           String?
  profileDescription String?
  email              String    @unique
  password           String
  remember           Boolean   @default(false)
  rememberToken      String?
  createdAt          DateTime  @default(now())
  updatedAt          DateTime? @updatedAt
  deletedAt          DateTime?

  @@map("users")
}
